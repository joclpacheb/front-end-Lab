export default class User {
  constructor (username, email, password, type, cedula, lastName, secondname) {
    this.username = username
    this.email = email
    this.password = password
    this.type = type
    this.cedula = cedula
    this.name = name
    this.lastName = lastName
    this.secondname = secondname
  }
}
